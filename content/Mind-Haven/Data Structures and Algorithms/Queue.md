Queue stores items in FIFO (First In First Out) order

Queue operation:
	Accessing the front of the queue takes $O(1)$
	Insertion to the back and deletion from the front of the queue takes $O(1)$, but we cannot insert or delete at other positions
