Stack stores items in LIFO (last in first out) order

Stack operations:
	Accessing the top of the stack takes $O(1)$
	Insertion and deletion from the top of the stack take $O(1)$, but we cannot insert or delete at other positions
